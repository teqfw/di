/**
 * Plugin constants (hardcoded configuration) for backend code.
 */
export default class TeqFw_Di_Back_Defaults {

    DESC_NODE = 'di'; // node in 'teqfw.json' descriptors

    constructor() {
        Object.freeze(this);
    }
}
