# Default CDC Profile Specification (`./ctx/docs/architecture/cdc-profile/default/`)

Path: `./ctx/docs/architecture/cdc-profile/default/AGENTS.md`

## Purpose

The `ctx/docs/architecture/cdc-profile/default/` directory defines the normative grammar, transformation rules, and validation rules of the Default CDC Profile supported by the product.

## Level Map

- `AGENTS.md` — this document, defining the purpose and structure of the default profile specification.
- `grammar.md` — default profile grammar, token model, and deterministic segmentation rules at the CDC surface.
- `transformation.md` — normative semantic mapping model from CDC surface form to structural canonical identity (`DepId`).
- `validation.md` — normative validation rules defining admissible default-profile CDC strings.

## Summary

`ctx/docs/architecture/cdc-profile/default/AGENTS.md` provides a navigational anchor for the Default CDC Profile specification.
