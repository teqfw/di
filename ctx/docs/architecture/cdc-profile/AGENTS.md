# CDC Profile Specification (`./ctx/docs/architecture/cdc-profile/`)

Path: `./ctx/docs/architecture/cdc-profile/AGENTS.md`

## Purpose

The `ctx/docs/architecture/cdc-profile/` directory defines normative CDC profile specifications as formal contracts for interpreting CDC strings into structural canonical dependency identity (`DepId`) at the parser boundary.

## Level Map

- `default/` — normative specification of the Default CDC Profile supported by the product.
- `AGENTS.md` — this document, defining the purpose and structure of CDC profile specifications at architecture level.

## Summary

`ctx/docs/architecture/cdc-profile/AGENTS.md` provides a navigational anchor for architecture-level CDC profile specifications.
