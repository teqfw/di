# Product Parser Documentation (`./ctx/docs/product/parser/`)

Path: `./ctx/docs/product/parser/AGENTS.md`

## Purpose

The `ctx/docs/product/parser/` directory contains product-level documentation of parser profiles shipped with or referenced by the product, including normative specification of the canonical default EDD profile.

## Level Map

- `default-profile/` — normative specification of the canonical default EDD profile shipped with the product.
- `AGENTS.md` — this document, defining the purpose and structure of the `parser/` directory.

## Summary

`ctx/docs/product/parser/AGENTS.md` provides a navigational anchor for product-level parser profile documentation without introducing architecture-level invariants.
