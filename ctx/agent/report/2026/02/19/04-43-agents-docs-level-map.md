# Iteration Report: Update `ctx/docs/AGENTS.md`

Goal: Update `ctx/docs/AGENTS.md` to reflect current `ctx/docs/` contents and ensure compliance with inherited ADSM level-map invariants.

Actions Performed:
- Reviewed inherited `AGENTS.md` requirements along the hierarchy (`AGENTS.md`, `ctx/AGENTS.md`) for mandatory `## Level Map` structure and ordering rules.
- Updated `ctx/docs/AGENTS.md` by adding a `## Level Map` section listing directories first (alphabetically) and the `AGENTS.md` file second, and updated the document version because invariants changed.

Artifacts Produced:
- Updated `ctx/docs/AGENTS.md`
